These scripts are meant to be called from the command line at the root of the project, from within a CoreOS VM, for example:

./scripts/deploy/install-dev.sh

(You can run CoreOS via [Vagrant](https://coreos.com/docs/running-coreos/platforms/vagrant/))
